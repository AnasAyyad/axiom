// Package emulator provides a deterministic local REST/WebSocket conformance
// server. It is test infrastructure and is intentionally not imported by the
// platform binary or configurable in a deployed runtime.
package emulator
