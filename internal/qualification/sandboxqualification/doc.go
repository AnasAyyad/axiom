// Package sandbox_qualification implements the fail-closed, observation-only sandbox qualification sandbox
// qualification contract. It never owns credentials or submits orders.
package sandboxQualification
