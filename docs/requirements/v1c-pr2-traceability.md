# V1C PR2 C4-C5 traceability

Status here distinguishes deterministic implementation/local qualification,
the two required operator-armed exchange canaries, and formal owner/security
acceptance. Both independent canary evidence files are sealed; this table does
not itself record formal acceptance.

| ID | Requirement | Implementation | Verification |
|---|---|---|---|
| AX-V1C-C04-001 | Binance authenticated traffic is fixed to Spot Testnet `/api/v3`; `/sapi`, production-private hosts, arbitrary routes, and prohibited products fail before signing | `internal/exchanges/binance/authenticated_operations.go`, authenticated policy/client, and `internal/egressproxy` | closed route captures, signing goldens, negative policies, source/security scans |
| AX-V1C-C04-002 | Exact filters, precision, rounding, clock health, coherent top-of-book eligibility, provider request weights, and single-snapshot reconciliation are enforced before entry | Binance sandbox filters, clock, rate, recovery, and eligibility files | decimal/filter/timestamp, bounded clock retry, conservative timestamp, single-read recovery, book, and rate-limit tests |
| AX-V1C-C04-003 | Account, open-order, history, fill, test-create, create, cancel, and query operations use neutral contracts and deterministic client IDs | `internal/exchanges/binance/sandbox_adapter.go`, snapshot, normalize, and authenticated operations | adapter contract, serializer, request-capture, and emulator tests |
| AX-V1C-C04-004 | The authenticated HMAC user-data stream durably normalizes balances and executions; reconnect backfills history | Binance private transport/stream/decoder and V1C private inbox stores | decoder fuzz, duplicate/late event, reconnect, reducer, and backfill tests |
| AX-V1C-C04-005 | Ambiguous create remains `UNKNOWN` and resolves by client ID before retry; cancellation/reconciliation capacity is preserved | Binance recovery/rate logic and shared unknown recovery | timeout, retry, query, cancellation, crash, and rate-budget tests |
| AX-V1C-C04-006 | A coherent Testnet reset increments epoch, records incident/external adjustment, locks entry, reconciles, and requires a fresh arm | Binance reset coordinator and V1C account recovery store | reset model, persistence, epoch, and re-arm tests |
| AX-V1C-C05-001 | Bybit authenticated traffic is fixed to Demo REST/private WebSocket; production hosts are credential-free public metadata/book sources only | Bybit authenticated policy/client, private transport, public adapter, and egress policies | endpoint capture, host separation, signing, negative policy, and security scans |
| AX-V1C-C05-002 | Key inspection accepts only Spot-only or ADR-0022's exact Bybit Demo UI Unified Trading bundle and rejects every partial, expanded, fund-management, or unknown nonempty permission set; wallet, Spot create/cancel/query/history, executions, time, and instrument metadata remain closed operations | Bybit authenticated operations, wallet/history/filter/clock files and ADR-0022 | permission-profile, category, leverage, order-filter, serializer, timestamp, and emulator tests |
| AX-V1C-C05-003 | REST create/cancel are asynchronous acknowledgements; private events and reconciliation are authoritative | Bybit adapter, decoder, inbox/reducer, and recovery stores | acknowledgement, duplicate Filled, cancel/fill race, history, and reconciliation tests |
| AX-V1C-C05-004 | Private WebSocket authenticates by the documented Demo protocol and subscribes only to `order.spot`, `execution.spot`, and `wallet`; WebSocket order entry is absent | Bybit private transport/stream/decoder | topic capture, authentication golden, reconnect/backfill, decoder fuzz, and prohibited-operation scans |
| AX-V1C-C05-005 | Every Bybit order is Spot, `isLeverage=0`, and `orderFilter=Order`; market/FOK/amend/batch/conditional/TP-SL/fund-management/derivative paths fail before signing | Bybit request construction and authenticated policy | exhaustive negative capture and serializer tests |
| AX-V1C-PR2-001 | Binance and Bybit engines have separate credentials, DB roles, leases, networks, and CONNECT-only proxies; other services and proxies receive no exchange credentials | `docker-compose.yml`, engine bootstrap, migration `000023`, and deployment config | all 1,024 Compose profile renders, command/secret/network contract, role, startup, and security tests |
| AX-V1C-PR2-002 | Each engine starts `LOCKED`, verifies identity/epoch/integrity, replays inbox, reconciles, proves public/private health, and reaches `READY_PAUSED` under a fencing lease | sandbox engine role, attestation, boundary, loop, and runtime stores | ordered startup, crash recovery, lease overlap, fencing, restart, and PostgreSQL tests |
| AX-V1C-PR2-003 | New entry retains all four enablement gates and current entry-safety checks; cancellation, query, reconciliation, recovery, and private events remain available while entry is blocked | engine dispatcher/command loop and V1C command/observation stores | arm-expiry, disabled-entry, stale-observation, cancellation, and recovery tests |
| AX-V1C-PR2-004 | A manually armed canary is buy-only, at most 10 USDT, and uses intent → allocator → central risk → planner → durable dispatcher | `internal/sandbox/canary.go` and sandbox canary bootstrap files | builder, protected-input, cap/style, authorization, and durable-path tests |
| AX-V1C-PR2-005 | Each exchange canary queries, cancels or observes fill, reconciles, survives a controlled engine restart, and proves exactly one create attempt | V1C engine command, canary read/store/evidence, and one-shot coordinator services | deterministic/emulator tests and both independent real canaries passed; each sealed record proves one authenticated create and one outbox attempt |
| AX-V1C-PR2-006 | Canary evidence is immutable, excludes secrets/private values, states that profitability is false, and identifies the exact configuration, build, and running executable | `internal/bootstrap/sandbox_canary_verify.go`, `internal/sandbox/canary_evidence.go`, and migration `000023` | atomic no-overwrite/file-mode tests and evidence completeness checks passed; Binance evidence `22072e94ccd24ee10094068ca74720479ba2362b374efac961751f23f4fc3473` and Bybit evidence `e39883d4f5e0650b3861b2c3cd753ba1fc832f0536902c9bcbf357568dfa765b` are sealed |
| AX-V1C-PR2-007 | Clean PostgreSQL 18 installation and exact B8 upgrade preserve role, fencing, command, observation, and immutable evidence constraints | migration `000023` and V1C PostgreSQL stores | dedicated clean-install and exact-upgrade qualification |

C6 API/console work and the concurrent 72-hour V1C qualification remain
outside PR2. Historical A/B evidence does not satisfy any row above.
