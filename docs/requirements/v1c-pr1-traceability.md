# V1C PR1 C1-C3 traceability

Status values distinguish implementation from verification. A checked source
mapping is not a sandbox canary, a 72-hour soak, or formal V1C acceptance.

| ID | Requirement | Implementation | Verification |
|---|---|---|---|
| AX-V1C-C01-001 | Testnet/Demo-only identities, spot-only policy, default-off global and per-exchange gates, fixed caps/styles/strategies, and advisory-only rebalancing | `internal/config/v1c.go`, `internal/config/v1c_validation.go`, `internal/sandbox/contracts.go` | configuration mutation and contract tests |
| AX-V1C-C01-002 | Authenticated clients remain separate from credential-free public adapters and expose no arbitrary request surface | authenticated client and policy files under `internal/exchanges/binance` and `internal/exchanges/bybit` | closed route-matrix and negative-policy tests |
| AX-V1C-C01-003 | Fixed authenticated hosts, routes, methods, fields, products, and styles; production-private and forbidden capabilities rejected before signing | authenticated policy files and `scripts/check-v1c-security-boundary.sh` | signing, exhaustive request-capture, source, binary, endpoint, and prohibited-capability gates |
| AX-V1C-C01-004 | Binance authentication is HMAC SHA-256 only and authenticated serializers remain unexported | `internal/exchanges/binance/authenticated_client.go` | official-format golden and package-boundary tests |
| AX-V1C-C01-005 | Separate CONNECT-only, port-443 proxy policies reject redirects, invalid addresses, rebinding, userinfo, fragments, IP literals, and suffix tricks | `internal/egressproxy`, `docker-compose.yml` | proxy policy, DNS-change, and 256-profile Compose-render tests |
| AX-V1C-C01-006 | Exchange credentials use five fixed file-reference variables; raw credentials and endpoint/proxy overrides are rejected | `internal/sandbox/credentials.go`, `internal/config/secret_validation.go`, `internal/config/v1c_validation.go` | environment mutation, secret, source, and Compose capture tests |
| AX-V1C-C01-007 | Request evidence is redacted, field-name bounded, durable, immutable, and fail-closed before network I/O | `internal/exchanges/contracts/authenticated.go`, authenticated clients, `internal/storage/postgres/v1c_authenticated_evidence_store.go`, migration `000022` | evidence validation, sink-failure, PostgreSQL policy/immutability, and unsafe direct-insert tests |
| AX-V1C-C01-008 | Binance validates Testnet account identity, key fingerprint, spot/HMAC capability, and owner attestation; Bybit requires read-write `SpotTrade` and rejects prohibited permissions | authenticated client files | startup identity and negative-permission tests |
| AX-V1C-C01-009 | Deterministic authenticated emulators cover accepted and rejected captures without using live credentials | `internal/exchanges/sandboxemulator` | emulator determinism and production-private negative-capture tests |
| AX-V1C-C02-001 | Existing Argon2id password, opaque session, session-limit, server-revocation, CSRF, and Origin boundaries remain intact | `internal/authentication`, existing API middleware | authentication unit/race tests and cumulative API tests |
| AX-V1C-C02-002 | RFC 6238 SHA-1, six-digit, 30-second TOTP accepts only a one-step window and keeps the seed API-only | `internal/authentication/totp.go`, `internal/authentication/sandbox_authorization.go` | RFC vector, boundary, malformed-code, and secret-file tests |
| AX-V1C-C02-003 | The last-used TOTP counter is persisted monotonically to prevent replay | migration `000021`, `internal/storage/postgres/v1c_auth_store.go` | replay model and PostgreSQL concurrency tests |
| AX-V1C-C02-004 | Password/TOTP reauthentication creates a hashed, purpose-bound, active-session/revision-bound, one-use grant that expires in two minutes | authentication authorization service, `internal/storage/postgres/v1c_auth_store.go`, and migration `000021` | password, expiry, stale/revoked session, session revision, purpose mismatch, consume rollback, and replay tests |
| AX-V1C-C02-005 | High-risk purposes are closed to sandbox arm, risk unlock, credential rotation, and revoke-all | `internal/authentication/sandbox_authorization.go`, migration `000021` | authorization-purpose and database constraint tests |
| AX-V1C-C02-006 | Owner receives read/arm/cancel/admin; viewer receives read only | migration `000021`, `internal/storage/postgres/a11_auth_store.go` | clean-install, exact-upgrade, and RBAC tests |
| AX-V1C-C02-007 | High-risk attempts/results are redacted and serialized into a hash-linked audit chain with before/after state hashes for revoke-all and credential rotation | authentication authorization service, `internal/storage/postgres/v1c_audit_store.go`, session-control and rotation stores | rejected-attempt, consume, revoke-all state-hash, rotation completion, and concurrent audit-chain tests |
| AX-V1C-C02-008 | Credential rotation is lock-first, quarantines nonterminal work, exposes no secret-write API, increments generation, reconciles, and ends `READY_PAUSED` | `internal/sandbox/credential_rotation.go`, `internal/storage/postgres/v1c_rotation_store.go` | rotation model, authorization-binding, and PostgreSQL transition tests |
| AX-V1C-C03-001 | The existing synchronous simulated broker remains separate from the asynchronous sandbox dispatcher | `internal/sandbox/dispatcher.go`; existing `internal/execution` broker contracts | package-boundary and dispatcher tests |
| AX-V1C-C03-002 | Plan, pipeline proof, eligibility, per-account entry-safety snapshot, reservations, deterministic client IDs, request hashes, non-refundable daily cap, saga, and fenced outbox persist atomically and bind the approval hash | memory/PostgreSQL dispatcher repositories, `v1c_plan_entry_safety`, and migration `000022` | approval-tamper, entry-safety persistence, atomic rollback, saga, and crash tests |
| AX-V1C-C03-003 | One capacity-bearing order per account, two globally, 10 USDT/order, and 50 USDT/UTC day are immutable upper bounds | configuration validation, dispatcher repositories, and migration `000022` triggers | cap property/model, concurrency, and direct-SQL guard tests |
| AX-V1C-C03-004 | Pending, claimed/open, acknowledged, partially filled, durable cancel-pending, `UNKNOWN`, cancel-ambiguous, and recovery states cannot release capacity unsafely | dispatcher cancellation repositories, outbox/reducer stores, and migration `000022` transition guards | cancel/fill race, network-crash retry, order-state, cap, ambiguous-timeout, and reservation-release tests |
| AX-V1C-C03-005 | Account-specific fencing leases own outbox claims, stale fences fail closed, and lease acquisition is crash-testable before and after its durable transition | `internal/sandbox/dispatcher.go`, `internal/storage/postgres/v1c_lease_store.go`, and outbox stores | lease overlap/loss/race plus pre-commit rollback and post-commit fence-persistence tests |
| AX-V1C-C03-006 | REST acknowledgements and private events enter a durable normalized inbox before the canonical reducer; event/fill/client-ID uniqueness is enforced | private inbox/reducer stores and migration `000022` | duplicate, late-event, fill, committed-inbox, and reducer tests |
| AX-V1C-C03-007 | Paired cross-exchange plans persist both reservations and exchange-owned legs in one transaction | plan approval transaction and existing execution saga | paired-plan atomicity, topology, capacity, and race tests |
| AX-V1C-C03-008 | Exchange balances/history are authoritative; coherent resets create a new epoch, non-P&L external adjustments, lock, and re-arm requirement | `internal/sandbox/account_recovery.go`, PostgreSQL recovery stores, migration `000022` | reset model, direct-SQL integrity, and reconciliation tests |
| AX-V1C-C03-009 | `UNKNOWN` orders retain reservations and capacity until client ID, history, private events, fills, and balances resolve or quarantine them | `internal/sandbox/unknown_recovery.go`, memory/PostgreSQL recovery stores | unknown recovery, duplicate evidence, and unresolved quarantine tests |
| AX-V1C-C03-010 | Eleven ordered startup stages acquire a lease, reconcile under `LOCKED`, consume the combined public `Eligible` snapshot, start private events, and stop at `READY_PAUSED` | `internal/sandbox/startup.go` | ordered, out-of-order, evidence-failure, eligibility, and degradation tests |
| AX-V1C-C03-011 | New entries require active arm/session/account state, four enablement gates, public/private/account/reconciliation/lease/evidence health, and cap availability at approval, outbox claim, and immediately before network submission; cancellation remains available while entry is blocked | startup/control contracts, dispatcher entry-safety proof, memory claim checks, and PostgreSQL plan/outbox stores | entry matrix, arm-expiry at claim/pre-network, revoked actor session, degradation, and cancel-availability tests |
| AX-V1C-C03-012 | Canary and eligible-strategy submissions share intent, allocator, risk, planner, asset-approval, saga, and durable-dispatch proof | approval pipeline contracts and dispatcher repositories | pipeline-binding, strategy-identity, paired-plan, and tamper tests |
| AX-V1C-C03-013 | Crash injection covers every named commit, network, acknowledgement, inbox, reducer, fill, reservation, and lease boundary | `KillBoundary` contract | exhaustive model kill-point and committed-inbox recovery suites |
| AX-V1C-C03-014 | Clean PostgreSQL install and exact B8-to-V1C upgrade preserve database-level immutability, uniqueness, and authorization guards | migrations `000021` and `000022` | isolated PostgreSQL 18.4 clean/upgrade gates |

PR1 does not implement C4/C5 exchange adapter completeness, sandbox canaries,
the C6 console, or the V1C 72-hour qualification.
