# B7 local validation evidence

## Status

B7 implementation is present at the current working checkpoint. Targeted Go
research tests, the independent Python checker, PostgreSQL 18 clean install,
and exact B6-to-B7 upgrade qualification have passed during implementation.
The final cumulative B4+B5+B6+B7 qualification, committed-source image,
supply-chain scans, and image-backed Compose smoke have not yet been run, so B7
is not yet recorded as locally verified in this revision.

Formal acceptance is not claimed. It remains held by A7/V1A, B1/B2 deferred
72-hour qualifications, formal predecessor acceptance, and Product, Security,
QA, and SRE approval. Research maturity is not evidence or a guarantee of
production profitability.

## Implemented authority and safety

- `research-preregistration.v1` canonically fixes the generation, strategy
  version, parameter search, chronological windows, models, benchmarks,
  thresholds, rules, seed, and registration time before final testing.
- `multi-strategy-validation.v1` binds one-use final-test consumption to
  walk-forward, bootstrap, neighborhood, capacity, stress, benchmark, regime,
  multiple-testing, Sharpe, source, and measurable-criterion evidence.
- The dependency-free Python 3.12.3 checker independently recalculates the B7
  statistical results and maturity eligibility against a committed Go/Python
  golden.
- Only formal Tier A primary backtest, replay, and shadow evidence can qualify.
  Paper, demo, testnet, Tier B, low-confidence, and integration-only evidence
  cannot be primary promotion evidence.
- Promotion is an explicit recent-reauthenticated `research.promote` command
  with immutable evidence, expected revision, idempotency, PostgreSQL-side
  authorization, one-winner concurrency, and applied/rejected audit evidence.
- B7 adds no broker, exchange credential, external order, transfer, withdrawal,
  accounting, or portfolio mutation authority.

## Qualification still required

- Commit the implementation checkpoint and rerun all B4-B7 model, race, fuzz,
  benchmark, SQL generation, clean/upgrade PostgreSQL, independent research,
  source-boundary, and cumulative repository checks from that source.
- Build and inspect the exact committed-source image with `DIRTY=false`, prove
  reproducibility, pass an isolated image-backed Compose smoke, generate the
  SPDX SBOM, and pass the exact HIGH/CRITICAL Trivy gate.
- Replace this provisional section with exact source, toolchain, timings,
  benchmark, image, SBOM, and scanner identities.
