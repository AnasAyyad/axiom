openapi: 3.0.3
info:
  title: Axiom V1B API
  version: 1.1.0
  description: >-
    Authenticated multi-exchange research, replay, and production-public shadow
    operations. Axiom V1B is simulation software and cannot place external
    orders or move external assets.
servers:
  - url: /
tags:
  - name: Health
  - name: Session
  - name: System
  - name: Binance
  - name: Exchanges
  - name: Opportunities
  - name: Portfolio
  - name: Inventory
  - name: Risk
  - name: Trend
  - name: Strategies
  - name: Rebalancing
  - name: Research
  - name: Reports
  - name: Backtest
  - name: Replay
  - name: Shadow
  - name: Incident
  - name: Audit
  - name: Stream
paths:
  /health/live:
    get:
      operationId: getLiveness
      tags: [Health]
      summary: Report whether the process loop can respond
      responses:
        "200":
          description: Process is live
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HealthResponse" }
  /health/ready:
    get:
      operationId: getReadiness
      tags: [Health]
      summary: Report whether all role prerequisites are ready
      responses:
        "200":
          description: Role is ready
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HealthResponse" }
        "503":
          $ref: "#/components/responses/HealthUnavailable"
  /api/v1/session/login:
    post:
      operationId: loginSession
      tags: [Session]
      summary: Authenticate and rotate to a fresh opaque session
      parameters:
        - $ref: "#/components/parameters/Origin"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LoginRequest" }
      responses:
        "201":
          description: Authenticated session created
          headers:
            Set-Cookie:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LoginResponse" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "503": { $ref: "#/components/responses/Unavailable" }
  /api/v1/session/logout:
    post:
      operationId: logoutSession
      tags: [Session]
      summary: Revoke the current session and clear its cookies
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
      responses:
        "204": { description: Session revoked }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/session/me:
    get:
      operationId: getCurrentSession
      tags: [Session]
      security: [{ sessionCookie: [] }]
      responses:
        "200":
          description: Current user and session projection
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionMe" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/system/version:
    get:
      operationId: getVersion
      tags: [System]
      responses:
        "200":
          description: Version identity
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VersionResponse" }
  /api/v1/system/build:
    get:
      operationId: getBuildInformation
      tags: [System]
      responses:
        "200":
          description: Non-secret build identity
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BuildInformation" }
  /api/v1/system/status:
    get:
      operationId: getSystemStatus
      tags: [System]
      security: [{ sessionCookie: [] }]
      responses:
        "200":
          description: Current fail-closed system projection
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SystemStatus" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/system/health:
    get:
      operationId: getDetailedHealth
      tags: [System]
      summary: Return internal redacted dependency detail on the metrics network
      security: [{ operationalBearer: [] }]
      responses:
        "200":
          description: Redacted detailed health
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DetailedHealthResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "503":
          description: A dependency is unavailable
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DetailedHealthResponse" }
  /api/v1/exchanges/binance/health:
    get:
      operationId: getBinanceHealth
      tags: [Binance]
      security: [{ sessionCookie: [] }]
      responses:
        "200":
          description: Production-public Binance health projection
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BinanceHealth" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/exchanges/binance/instruments:
    get:
      operationId: listBinanceInstruments
      tags: [Binance]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: Deterministically ordered Spot metadata
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InstrumentPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/exchanges:
    get:
      operationId: listExchanges
      tags: [Exchanges]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: Generic production-public exchange quality projections
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ExchangePage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/opportunities:
    get:
      operationId: listOpportunities
      tags: [Opportunities]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: kind
          schema: { type: string, enum: [triangular, cross_exchange] }
      responses:
        "200":
          description: Durable triangular and cross-exchange scanner snapshot
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OpportunityPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/opportunities/{id}:
    get:
      operationId: getOpportunity
      tags: [Opportunities]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceID"
      responses:
        "200":
          description: Multi-leg evidence, inventory impact, and recovery analysis
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OpportunityDetail" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/portfolios:
    get:
      operationId: listPortfolios
      tags: [Portfolio]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: Virtual portfolio summaries
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PortfolioPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/portfolios/{id}:
    get:
      operationId: getPortfolio
      tags: [Portfolio]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceID"
      responses:
        "200":
          description: Exact virtual balance and position projection
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PortfolioDetail" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/portfolios/{id}/journal:
    get:
      operationId: listPortfolioJournal
      tags: [Portfolio]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceID"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: Exact immutable virtual journal page
          content:
            application/json:
              schema: { $ref: "#/components/schemas/JournalPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/inventory:
    get:
      operationId: listInventory
      tags: [Inventory]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: exchange
          schema: { type: string, maxLength: 32 }
        - in: query
          name: asset
          schema: { type: string, maxLength: 16 }
        - in: query
          name: strategy
          schema: { type: string, maxLength: 128 }
        - in: query
          name: portfolio
          schema: { type: string, maxLength: 192 }
      responses:
        "200":
          description: Isolated virtual inventory positions; never a combined balance
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InventoryPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/risk/status:
    get:
      operationId: getRiskStatus
      tags: [Risk]
      security: [{ sessionCookie: [] }]
      responses:
        "200":
          description: Effective global risk state and evidence
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RiskStatus" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/risk/pause:
    post:
      operationId: pauseRisk
      tags: [Risk]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        $ref: "#/components/requestBodies/RevisionCommand"
      responses:
        "202":
          description: Durable pause command accepted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CommandAccepted" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "412": { $ref: "#/components/responses/PreconditionFailed" }
  /api/v1/risk/resume:
    post:
      operationId: resumeRisk
      tags: [Risk]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        $ref: "#/components/requestBodies/RevisionCommand"
      responses:
        "202":
          description: Policy-gated durable resume command accepted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CommandAccepted" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "412": { $ref: "#/components/responses/PreconditionFailed" }
        "503": { $ref: "#/components/responses/Unavailable" }
  /api/v1/strategies/trend:
    get:
      operationId: getTrendStrategy
      tags: [Trend]
      security: [{ sessionCookie: [] }]
      responses:
        "200":
          description: Immutable Trend version, parameters, and evidence maturity
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TrendStatus" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/strategies/trend/decisions:
    get:
      operationId: listTrendDecisions
      tags: [Trend]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: outcome
          schema: { type: string, enum: [accepted, rejected] }
      responses:
        "200":
          description: Canonical decision and rejection explanations
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TrendDecisionPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/strategies:
    get:
      operationId: listStrategies
      tags: [Strategies]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: Generic strategy versions, modes, evidence, and maturity
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StrategyPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/rebalancing/recommendations:
    get:
      operationId: listRebalancingRecommendations
      tags: [Rebalancing]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: Read-only advisory rebalancing snapshot
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RebalancingPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/rebalancing/recommendations/{id}:
    get:
      operationId: getRebalancingRecommendation
      tags: [Rebalancing]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceID"
      responses:
        "200":
          description: Advisory route evidence and manual checklist
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RebalancingDetail" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/research/champion-challenger:
    get:
      operationId: listChampionChallengerReports
      tags: [Research]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: Immutable research comparison reports without trading authority
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ChampionChallengerPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/reports/{id}/exports:
    post:
      operationId: exportResearchReport
      tags: [Reports]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceID"
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReportExportRequest" }
      responses:
        "201":
          description: Immutable bounded report export created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ReportExportResource" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /api/v1/backtests:
    post:
      operationId: createBacktest
      tags: [Backtest]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OfflineJobRequest" }
      responses:
        "202":
          description: Durable backtest job accepted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/JobResource" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "503": { $ref: "#/components/responses/Unavailable" }
  /api/v1/backtests/{id}:
    get:
      operationId: getBacktest
      tags: [Backtest]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceID"
      responses:
        "200":
          description: Backtest lifecycle, result, and reproducibility projection
          content:
            application/json:
              schema: { $ref: "#/components/schemas/JobResource" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/replays:
    post:
      operationId: createReplay
      tags: [Replay]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReplayJobRequest" }
      responses:
        "202":
          description: Durable replay job accepted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/JobResource" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "429": { $ref: "#/components/responses/RateLimited" }
  /api/v1/replays/{id}:
    get:
      operationId: getReplay
      tags: [Replay]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceID"
        - in: query
          name: event_ordinal
          description: Select one persisted canonical replay event; defaults to the newest available event.
          schema: { $ref: "#/components/schemas/Revision" }
      responses:
        "200":
          description: Replay lifecycle and deterministic cursor projection
          content:
            application/json:
              schema: { $ref: "#/components/schemas/JobResource" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/replays/{id}/pause:
    post:
      operationId: pauseReplay
      tags: [Replay]
      security: [{ sessionCookie: [] }]
      parameters: &replayCommandParameters
        - $ref: "#/components/parameters/ResourceID"
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        $ref: "#/components/requestBodies/RevisionCommand"
      responses: &commandResponses
        "202":
          description: Durable command accepted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CommandAccepted" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "412": { $ref: "#/components/responses/PreconditionFailed" }
  /api/v1/replays/{id}/resume:
    post:
      operationId: resumeReplay
      tags: [Replay]
      security: [{ sessionCookie: [] }]
      parameters: *replayCommandParameters
      requestBody:
        $ref: "#/components/requestBodies/RevisionCommand"
      responses: *commandResponses
  /api/v1/replays/{id}/step:
    post:
      operationId: stepReplay
      tags: [Replay]
      security: [{ sessionCookie: [] }]
      parameters: *replayCommandParameters
      requestBody:
        $ref: "#/components/requestBodies/RevisionCommand"
      responses: *commandResponses
  /api/v1/replays/{id}/faults:
    get:
      operationId: listReplayFaults
      tags: [Replay]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceID"
      responses:
        "200":
          description: Immutable deterministic replay fault schedule
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ReplayFaultPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: scheduleReplayFault
      tags: [Replay]
      security: [{ sessionCookie: [] }]
      parameters: *replayCommandParameters
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReplayFaultRequest" }
      responses:
        "201":
          description: Fault schedule persisted before replay materialization
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ReplayFaultResource" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "412": { $ref: "#/components/responses/PreconditionFailed" }
  /api/v1/shadow-sessions:
    post:
      operationId: createShadowSession
      tags: [Shadow]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ShadowSessionRequest" }
      responses:
        "202":
          description: Public-data simulation-only shadow session accepted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShadowSessionResource" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "503": { $ref: "#/components/responses/Unavailable" }
  /api/v1/shadow-sessions/{id}/stop:
    post:
      operationId: stopShadowSession
      tags: [Shadow]
      security: [{ sessionCookie: [] }]
      parameters: *replayCommandParameters
      requestBody:
        $ref: "#/components/requestBodies/RevisionCommand"
      responses: *commandResponses
  /api/v1/shadow-sessions/{id}:
    get:
      operationId: getShadowSession
      tags: [Shadow]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceID"
      responses:
        "200":
          description: Public-data virtual shadow projection
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShadowSessionResource" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/incidents:
    get:
      operationId: listIncidents
      tags: [Incident]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: state
          schema: { type: string, enum: [open, acknowledged, resolved] }
      responses:
        "200":
          description: Correlated incident summaries
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IncidentPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/incidents/{id}:
    get:
      operationId: getIncident
      tags: [Incident]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceID"
        - in: query
          name: include_raw
          schema: { type: boolean, default: false }
      responses:
        "200":
          description: Correlated redacted incident timeline
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IncidentDetail" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/audit-events:
    get:
      operationId: listAuditEvents
      tags: [Audit]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: event_type
          schema: { type: string, maxLength: 80 }
        - in: query
          name: include_detail
          schema: { type: boolean, default: false }
      responses:
        "200":
          description: Redacted immutable audit page
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuditEventPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/sandbox/overview:
    get:
      operationId: getSandboxOverview
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      responses:
        "200":
          description: Authoritative redacted Testnet and Demo operations snapshot
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SandboxOverview" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "503": { $ref: "#/components/responses/Unavailable" }
  /api/v1/sandbox/orders:
    get:
      operationId: listSandboxOrders
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: exchange
          schema: { $ref: "#/components/schemas/SandboxExchange" }
        - in: query
          name: state
          schema: { type: string, maxLength: 40 }
      responses:
        "200":
          description: Redacted sandbox orders, attempts, and fills
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SandboxOrderPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createSandboxTestOrder
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SandboxTestOrderRequest" }
      responses:
        "202":
          description: Test-only order plan durably approved for the matching engine
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CommandAccepted" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "412": { $ref: "#/components/responses/PreconditionFailed" }
        "503": { $ref: "#/components/responses/Unavailable" }
  /api/v1/sandbox/reconciliations:
    get:
      operationId: listSandboxReconciliations
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: exchange
          schema: { $ref: "#/components/schemas/SandboxExchange" }
      responses:
        "200":
          description: Reconciliation, suspense, quarantine, and reset evidence
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SandboxReconciliationPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/sandbox/qualification:
    get:
      operationId: getC6QualificationStatus
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      responses:
        "200":
          description: Current deterministic chaos and C6 soak state
          content:
            application/json:
              schema: { $ref: "#/components/schemas/C6QualificationStatus" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/sandbox/authorizations:
    post:
      operationId: createSandboxAuthorization
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SandboxAuthorizationRequest" }
      responses:
        "201":
          description: One-use, session-bound high-risk authorization
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SandboxAuthorizationGrant" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "503": { $ref: "#/components/responses/Unavailable" }
  /api/v1/sandbox/sessions/{id}/arms:
    post:
      operationId: createSandboxArm
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceID"
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SandboxArmRequest" }
      responses:
        "201":
          description: Exact 15-minute audited arm
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SandboxArm" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "412": { $ref: "#/components/responses/PreconditionFailed" }
  /api/v1/sandbox/arms/{id}/revoke:
    post:
      operationId: revokeSandboxArm
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      parameters: &sandboxCommandParameters
        - $ref: "#/components/parameters/ResourceID"
        - $ref: "#/components/parameters/Origin"
        - $ref: "#/components/parameters/CSRFToken"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        $ref: "#/components/requestBodies/RevisionCommand"
      responses: &sandboxCommandResponses
        "202":
          description: Audited durable sandbox command accepted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CommandAccepted" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "412": { $ref: "#/components/responses/PreconditionFailed" }
        "503": { $ref: "#/components/responses/Unavailable" }
  /api/v1/sandbox/accounts/{id}/unlock:
    post:
      operationId: unlockSandboxAccount
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      parameters: *sandboxCommandParameters
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SandboxUnlockRequest" }
      responses: *sandboxCommandResponses
  /api/v1/sandbox/orders/{id}/cancel:
    post:
      operationId: cancelSandboxOrder
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      parameters: *sandboxCommandParameters
      requestBody:
        $ref: "#/components/requestBodies/RevisionCommand"
      responses: *sandboxCommandResponses
  /api/v1/sandbox/orders/{id}/query:
    post:
      operationId: querySandboxOrder
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      parameters: *sandboxCommandParameters
      requestBody:
        $ref: "#/components/requestBodies/RevisionCommand"
      responses: *sandboxCommandResponses
  /api/v1/sandbox/accounts/{id}/reconcile:
    post:
      operationId: reconcileSandboxAccount
      tags: [Sandbox]
      security: [{ sessionCookie: [] }]
      parameters: *sandboxCommandParameters
      requestBody:
        $ref: "#/components/requestBodies/RevisionCommand"
      responses: *sandboxCommandResponses
  /api/v1/stream:
    get:
      operationId: streamEvents
      tags: [Stream]
      summary: Resume durable operational events after a REST snapshot revision
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/Origin"
        - in: header
          name: Last-Event-ID
          schema: { $ref: "#/components/schemas/Revision" }
        - in: query
          name: after_revision
          schema: { $ref: "#/components/schemas/Revision" }
      responses:
        "200":
          description: Versioned SSE stream with 15-second heartbeats
          content:
            text/event-stream:
              schema:
                type: string
                description: Each data field is one StreamEvent JSON object.
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "410":
          description: Resume cursor expired; fetch fresh REST snapshots
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: axiom_session
    operationalBearer:
      type: http
      scheme: bearer
      bearerFormat: opaque
  parameters:
    Origin:
      in: header
      name: Origin
      required: true
      schema: { type: string, format: uri, maxLength: 512 }
    CSRFToken:
      in: header
      name: X-CSRF-Token
      required: true
      schema: { type: string, minLength: 32, maxLength: 512 }
    IdempotencyKey:
      in: header
      name: Idempotency-Key
      required: true
      schema:
        {
          type: string,
          minLength: 16,
          maxLength: 128,
          pattern: "^[A-Za-z0-9._:-]+$",
        }
    ResourceID:
      in: path
      name: id
      required: true
      schema: { type: string, minLength: 1, maxLength: 160 }
    Cursor:
      in: query
      name: cursor
      schema: { type: string, minLength: 16, maxLength: 1024 }
    PageSize:
      in: query
      name: page_size
      schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
  requestBodies:
    RevisionCommand:
      required: true
      content:
        application/json:
          schema: { $ref: "#/components/schemas/RevisionCommandRequest" }
  responses:
    BadRequest:
      description: Request failed safe validation
      content:
        { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
    Unauthorized:
      description: Authentication failed
      content:
        { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
    Forbidden:
      description: Authorization, Origin, or CSRF policy denied the request
      content:
        { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
    NotFound:
      description: Resource not found
      content:
        { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
    Conflict:
      description: Idempotency payload or lifecycle conflict
      content:
        { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
    PreconditionFailed:
      description: Expected entity revision is stale
      content:
        { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
    RateLimited:
      description: Durable authentication or workload quota exceeded
      content:
        { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
    Unavailable:
      description: A required fail-closed prerequisite is unavailable
      content:
        { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
    HealthUnavailable:
      description: A required dependency is unavailable
      content:
        application/json:
          schema: { $ref: "#/components/schemas/HealthResponse" }
  schemas:
    Decimal:
      type: string
      pattern: "^-?(0|[1-9][0-9]*)(\\.[0-9]+)?$"
      example: "1000.000000000000000000"
    NonnegativeDecimal:
      type: string
      pattern: "^(0|[1-9][0-9]*)(\\.[0-9]+)?$"
    Revision:
      type: string
      pattern: "^(0|[1-9][0-9]*)$"
    Timestamp:
      type: string
      format: date-time
      description: RFC 3339 timestamp with an explicit UTC offset.
    Error:
      type: object
      additionalProperties: false
      required: [code, message, correlation_id]
      properties:
        code: { type: string, minLength: 1, maxLength: 80 }
        message: { type: string, minLength: 1, maxLength: 240 }
        correlation_id: { type: string, minLength: 1, maxLength: 160 }
        field_details:
          type: object
          additionalProperties: { type: string, maxLength: 160 }
    Page:
      type: object
      additionalProperties: false
      required: [revision, has_more]
      properties:
        revision: { $ref: "#/components/schemas/Revision" }
        next_cursor: { type: string, minLength: 16, maxLength: 1024 }
        has_more: { type: boolean }
    HealthResponse:
      type: object
      additionalProperties: false
      required: [status, role, phase]
      properties:
        status: { type: string, enum: [live, ready, not_ready] }
        role: { type: string, minLength: 1 }
        phase: { type: string, enum: [A1, A11] }
        reason_code: { type: string, minLength: 1 }
    VersionResponse:
      type: object
      additionalProperties: false
      required: [version]
      properties:
        version: { type: string, minLength: 1 }
    BuildInformation:
      type: object
      additionalProperties: false
      required: [version, commit, built_at, go_version, dirty]
      properties:
        version: { type: string, minLength: 1 }
        commit: { type: string, minLength: 1 }
        built_at: { type: string, minLength: 1 }
        go_version: { type: string, minLength: 1 }
        dirty: { type: boolean }
    LoginRequest:
      type: object
      additionalProperties: false
      required: [email, password]
      properties:
        email: { type: string, format: email, minLength: 3, maxLength: 254 }
        password:
          { type: string, minLength: 1, maxLength: 1024, writeOnly: true }
    LoginResponse:
      type: object
      additionalProperties: false
      required: [user, csrf_token, expires_at]
      properties:
        user: { $ref: "#/components/schemas/SessionUser" }
        csrf_token: { type: string, minLength: 32, maxLength: 512 }
        expires_at: { $ref: "#/components/schemas/Timestamp" }
    SessionUser:
      type: object
      additionalProperties: false
      required: [id, email, roles, permissions]
      properties:
        id: { type: string }
        email: { type: string, format: email }
        roles: { type: array, items: { type: string }, maxItems: 16 }
        permissions: { type: array, items: { type: string }, maxItems: 64 }
    SessionMe:
      type: object
      additionalProperties: false
      required: [user, session_id, session_revision, reauthenticated_at]
      properties:
        user: { $ref: "#/components/schemas/SessionUser" }
        session_id: { type: string }
        session_revision: { $ref: "#/components/schemas/Revision" }
        reauthenticated_at: { $ref: "#/components/schemas/Timestamp" }
    SystemStatus:
      type: object
      additionalProperties: false
      required:
        [
          release,
          phase,
          role,
          lifecycle_state,
          strategy_activation,
          real_trading_enabled,
        ]
      properties:
        release: { type: string, enum: [V1A, V1B] }
        phase: { type: string, enum: [A1, A11, B8] }
        role: { type: string, minLength: 1 }
        lifecycle_state:
          {
            type: string,
            enum: [STARTING, READY_PAUSED, RUNNING, DEGRADED, STOPPING],
          }
        strategy_activation: { type: string, enum: [unavailable, trend.v1a.1] }
        real_trading_enabled: { type: boolean, enum: [false] }
        environment: { type: string }
        execution_mode:
          { type: string, enum: [backtest, replay, paper, shadow] }
        engine_state: { type: string }
        binance_state: { type: string }
        risk_state: { type: string, enum: [PAUSED, RESUMED, LOCKED] }
        active_resource_id: { type: string }
        critical_incidents: { type: integer, minimum: 0 }
        server_time: { $ref: "#/components/schemas/Timestamp" }
        clock_drift_ms: { type: string, pattern: "^-?(0|[1-9][0-9]*)$" }
        revision: { $ref: "#/components/schemas/Revision" }
    DetailedHealthResponse:
      type: object
      additionalProperties: false
      required:
        [status, role, lifecycle_state, real_trading_enabled, components]
      properties:
        status: { type: string, enum: [ready, not_ready] }
        role: { type: string }
        lifecycle_state:
          {
            type: string,
            enum: [STARTING, READY_PAUSED, RUNNING, DEGRADED, STOPPING],
          }
        real_trading_enabled: { type: boolean, enum: [false] }
        components:
          type: array
          maxItems: 32
          items: { $ref: "#/components/schemas/HealthComponent" }
    HealthComponent:
      type: object
      additionalProperties: false
      required: [name, status]
      properties:
        name:
          {
            type: string,
            enum:
              [
                postgres,
                authentication,
                outbox,
                public_binance,
                disk,
                recovery,
              ],
          }
        status: { type: string, enum: [ready, not_ready] }
        reason_code:
          {
            type: string,
            enum:
              [
                required_dependency_unavailable,
                bootstrap_required,
                stale,
                locked,
              ],
          }
    BinanceHealth:
      type: object
      additionalProperties: false
      required:
        [
          environment,
          public_only,
          websocket_state,
          book_state,
          recorder_state,
          revision,
          observed_at,
        ]
      properties:
        environment: { type: string, enum: [production_public] }
        public_only: { type: boolean, enum: [true] }
        capabilities: { type: array, items: { type: string }, maxItems: 16 }
        websocket_state:
          { type: string, enum: [healthy, reconnecting, degraded, stale] }
        book_state: { type: string, enum: [healthy, rebuilding, gapped, stale] }
        clock_drift_ms: { type: string, pattern: "^-?(0|[1-9][0-9]*)$" }
        gaps: { type: integer, minimum: 0 }
        rebuilds: { type: integer, minimum: 0 }
        recorder_state:
          { type: string, enum: [healthy, degraded, paused, unavailable] }
        revision: { $ref: "#/components/schemas/Revision" }
        observed_at: { $ref: "#/components/schemas/Timestamp" }
    Instrument:
      type: object
      additionalProperties: false
      required:
        [
          id,
          symbol,
          product,
          price_tick,
          quantity_step,
          minimum_quantity,
          minimum_notional,
          metadata_version,
        ]
      properties:
        id: { type: string }
        symbol: { type: string }
        product: { type: string, enum: [spot] }
        price_tick: { $ref: "#/components/schemas/NonnegativeDecimal" }
        quantity_step: { $ref: "#/components/schemas/NonnegativeDecimal" }
        minimum_quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        minimum_notional: { $ref: "#/components/schemas/NonnegativeDecimal" }
        metadata_version: { $ref: "#/components/schemas/Revision" }
    InstrumentPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [items]
          properties:
            items:
              {
                type: array,
                items: { $ref: "#/components/schemas/Instrument" },
              }
    QualityEvidence:
      type: object
      additionalProperties: false
      required:
        [tier, confidence, freshness, source, observed_at, provenance_complete]
      properties:
        tier:
          {
            type: string,
            enum: [formal_tier_a, local_tier_b, integration_only, unknown],
          }
        confidence:
          { type: string, enum: [high, medium, low, insufficient, unknown] }
        freshness:
          { type: string, enum: [live, fresh, stale, historical, expired] }
        source: { type: string, minLength: 1, maxLength: 192 }
        observer: { type: string, maxLength: 192 }
        observed_at: { $ref: "#/components/schemas/Timestamp" }
        expires_at: { $ref: "#/components/schemas/Timestamp" }
        provenance_complete: { type: boolean }
        warnings:
          { type: array, items: { type: string, maxLength: 500 }, maxItems: 64 }
    ExchangeSummary:
      type: object
      additionalProperties: false
      required:
        [
          id,
          name,
          environment,
          public_only,
          websocket_state,
          book_state,
          recorder_state,
          capabilities,
          instruments,
          quality,
          revision,
        ]
      properties:
        id: { type: string }
        name: { type: string }
        environment: { type: string, enum: [production_public] }
        public_only: { type: boolean, enum: [true] }
        websocket_state:
          { type: string, enum: [healthy, reconnecting, degraded, stale] }
        book_state: { type: string, enum: [healthy, rebuilding, gapped, stale] }
        recorder_state:
          { type: string, enum: [healthy, degraded, paused, unavailable] }
        capabilities: { type: array, items: { type: string }, maxItems: 32 }
        instruments: { type: integer, minimum: 0 }
        last_message_age_ms: { $ref: "#/components/schemas/Revision" }
        sequence_gaps: { type: integer, minimum: 0 }
        reconnects: { type: integer, minimum: 0 }
        quality: { $ref: "#/components/schemas/QualityEvidence" }
        revision: { $ref: "#/components/schemas/Revision" }
    ExchangePage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [snapshot_revision, items]
          properties:
            snapshot_revision: { $ref: "#/components/schemas/Revision" }
            items:
              {
                type: array,
                items: { $ref: "#/components/schemas/ExchangeSummary" },
              }
    OpportunitySummary:
      type: object
      additionalProperties: false
      required:
        [
          id,
          kind,
          label,
          strategy_version,
          tested_size,
          maximum_size,
          gross_metric,
          net_metric,
          expected_profit,
          worst_case_profit,
          status,
          simulation_only,
          quality,
          recorded_at,
          revision,
        ]
      properties:
        id: { type: string }
        kind: { type: string, enum: [triangular, cross_exchange] }
        label: { type: string }
        instrument: { type: string }
        cycle_path:
          { type: array, items: { type: string }, minItems: 2, maxItems: 8 }
        strategy_version: { type: string }
        buy_exchange: { type: string }
        sell_exchange: { type: string }
        exchange: { type: string }
        tested_size: { $ref: "#/components/schemas/NonnegativeDecimal" }
        maximum_size: { $ref: "#/components/schemas/NonnegativeDecimal" }
        gross_metric: { $ref: "#/components/schemas/Decimal" }
        net_metric: { $ref: "#/components/schemas/Decimal" }
        expected_profit: { $ref: "#/components/schemas/Decimal" }
        worst_case_profit: { $ref: "#/components/schemas/Decimal" }
        opportunity_age_nanos: { $ref: "#/components/schemas/Revision" }
        lifetime_nanos: { $ref: "#/components/schemas/Revision" }
        status:
          {
            type: string,
            enum:
              [
                detected,
                qualified,
                simulated,
                recovery_required,
                quarantined,
                rejected,
                expired,
              ],
          }
        simulation_only: { type: boolean, enum: [true] }
        quality: { $ref: "#/components/schemas/QualityEvidence" }
        recorded_at: { $ref: "#/components/schemas/Timestamp" }
        revision: { $ref: "#/components/schemas/Revision" }
    OpportunityPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [snapshot_revision, items]
          properties:
            snapshot_revision: { $ref: "#/components/schemas/Revision" }
            items:
              {
                type: array,
                items: { $ref: "#/components/schemas/OpportunitySummary" },
              }
    OpportunityLeg:
      type: object
      additionalProperties: false
      required:
        [
          index,
          exchange,
          instrument,
          side,
          input_quantity,
          trade_quantity,
          gross_output,
          net_output,
          fee_asset,
          fee_quantity,
          fee_quote_equivalent,
          vwap,
          depth_cost,
          state,
          revision,
        ]
      properties:
        index: { type: integer, minimum: 0, maximum: 7 }
        exchange: { type: string }
        instrument: { type: string }
        side: { type: string, enum: [buy, sell] }
        source_asset: { type: string }
        target_asset: { type: string }
        input_quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        trade_quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        gross_output: { $ref: "#/components/schemas/NonnegativeDecimal" }
        net_output: { $ref: "#/components/schemas/NonnegativeDecimal" }
        fee_asset: { type: string }
        fee_quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        fee_quote_equivalent:
          { $ref: "#/components/schemas/NonnegativeDecimal" }
        vwap: { $ref: "#/components/schemas/NonnegativeDecimal" }
        depth_cost: { $ref: "#/components/schemas/NonnegativeDecimal" }
        arrival_offset_nanos: { $ref: "#/components/schemas/Revision" }
        state: { type: string }
        revision: { $ref: "#/components/schemas/Revision" }
    InventoryImpact:
      type: object
      additionalProperties: false
      required:
        [exchange, asset, before, after, band_state, natural_reverse_preferred]
      properties:
        exchange: { type: string }
        asset: { type: string }
        before: { $ref: "#/components/schemas/NonnegativeDecimal" }
        after: { $ref: "#/components/schemas/NonnegativeDecimal" }
        band_state: { type: string }
        natural_reverse_preferred: { type: boolean }
    RecoveryAnalysis:
      type: object
      additionalProperties: false
      required:
        [
          attempted,
          succeeded,
          quarantined,
          disposition,
          recovery_loss,
          explanation,
        ]
      properties:
        attempted: { type: boolean }
        succeeded: { type: boolean }
        quarantined: { type: boolean }
        disposition: { type: string }
        recovery_loss: { $ref: "#/components/schemas/Decimal" }
        explanation: { type: string, maxLength: 1000 }
    EvidenceTimelineEvent:
      type: object
      additionalProperties: false
      required:
        [index, event_type, label, occurred_at, correlation_id, revision]
      properties:
        index: { type: integer, minimum: 0 }
        event_type: { type: string }
        label: { type: string, maxLength: 500 }
        occurred_at: { $ref: "#/components/schemas/Timestamp" }
        correlation_id: { type: string }
        revision: { $ref: "#/components/schemas/Revision" }
    OpportunityDetail:
      type: object
      additionalProperties: false
      required:
        [
          summary,
          legs,
          inventory,
          recovery,
          cost_attribution,
          timeline,
          raw_evidence_available,
        ]
      properties:
        summary: { $ref: "#/components/schemas/OpportunitySummary" }
        legs:
          {
            type: array,
            items: { $ref: "#/components/schemas/OpportunityLeg" },
          }
        inventory:
          {
            type: array,
            items: { $ref: "#/components/schemas/InventoryImpact" },
          }
        recovery: { $ref: "#/components/schemas/RecoveryAnalysis" }
        cost_attribution:
          type: object
          additionalProperties: { $ref: "#/components/schemas/Decimal" }
        timeline:
          {
            type: array,
            items: { $ref: "#/components/schemas/EvidenceTimelineEvent" },
          }
        raw_evidence_available: { type: boolean }
    StrategySummary:
      type: object
      additionalProperties: false
      required:
        [
          id,
          family,
          name,
          version,
          supported_modes,
          maturity,
          evidence_role,
          confidence,
          viability,
          disclaimer,
          revision,
          created_at,
        ]
      properties:
        id: { type: string }
        family: { type: string }
        name: { type: string }
        version: { type: string }
        supported_modes:
          type: array
          items: { type: string, enum: [backtest, replay, shadow] }
          minItems: 1
          maxItems: 3
        maturity:
          {
            type: string,
            enum:
              [
                EXPERIMENTAL,
                BACKTEST_VALIDATED,
                REPLAY_VALIDATED,
                SHADOW_VALIDATED,
                REJECTED,
              ],
          }
        evidence_role:
          { type: string, enum: [champion, challenger, unassigned] }
        confidence:
          {
            type: string,
            enum: [formal_tier_a, local_tier_b, insufficient, rejected],
          }
        viability:
          {
            type: string,
            enum: [undetermined, viable_for_more_research, rejected],
          }
        primary_metric: { type: string }
        disclaimer: { type: string }
        revision: { $ref: "#/components/schemas/Revision" }
        created_at: { $ref: "#/components/schemas/Timestamp" }
    StrategyPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [snapshot_revision, items]
          properties:
            snapshot_revision: { $ref: "#/components/schemas/Revision" }
            items:
              {
                type: array,
                items: { $ref: "#/components/schemas/StrategySummary" },
              }
    InventoryPosition:
      type: object
      additionalProperties: false
      required:
        [
          id,
          portfolio_id,
          experiment_id,
          strategy_version,
          exchange,
          asset,
          available,
          reserved,
          before,
          after,
          status,
          virtual,
          quality,
          updated_at,
          revision,
        ]
      properties:
        id: { type: string }
        portfolio_id: { type: string }
        experiment_id: { type: string }
        strategy_version: { type: string }
        exchange: { type: string }
        asset: { type: string }
        available: { $ref: "#/components/schemas/NonnegativeDecimal" }
        reserved: { $ref: "#/components/schemas/NonnegativeDecimal" }
        before: { $ref: "#/components/schemas/NonnegativeDecimal" }
        after: { $ref: "#/components/schemas/NonnegativeDecimal" }
        target: { $ref: "#/components/schemas/NonnegativeDecimal" }
        minimum_band: { $ref: "#/components/schemas/NonnegativeDecimal" }
        maximum_band: { $ref: "#/components/schemas/NonnegativeDecimal" }
        cost_basis: { $ref: "#/components/schemas/NonnegativeDecimal" }
        market_value: { $ref: "#/components/schemas/NonnegativeDecimal" }
        inventory_pnl: { $ref: "#/components/schemas/Decimal" }
        status: { type: string }
        virtual: { type: boolean, enum: [true] }
        quality: { $ref: "#/components/schemas/QualityEvidence" }
        updated_at: { $ref: "#/components/schemas/Timestamp" }
        revision: { $ref: "#/components/schemas/Revision" }
    InventoryPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required:
            [snapshot_revision, combined_balance, isolation_notice, items]
          properties:
            snapshot_revision: { $ref: "#/components/schemas/Revision" }
            combined_balance: { type: boolean, enum: [false] }
            isolation_notice: { type: string, minLength: 1, maxLength: 500 }
            items:
              {
                type: array,
                items: { $ref: "#/components/schemas/InventoryPosition" },
              }
    RebalancingSummary:
      type: object
      additionalProperties: false
      required:
        [
          id,
          method,
          source_exchange,
          source_asset,
          destination_exchange,
          destination_asset,
          quantity,
          total_cost,
          minimum_duration_nanos,
          maximum_duration_nanos,
          risk_score,
          warnings,
          advisory_only,
          quality,
          recorded_at,
          revision,
        ]
      properties:
        id: { type: string }
        method:
          {
            type: string,
            enum: [natural_reverse_arbitrage, reviewed_graph_route],
          }
        source_exchange: { type: string }
        source_asset: { type: string }
        destination_exchange: { type: string }
        destination_asset: { type: string }
        quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        total_cost: { $ref: "#/components/schemas/NonnegativeDecimal" }
        minimum_duration_nanos: { $ref: "#/components/schemas/Revision" }
        maximum_duration_nanos: { $ref: "#/components/schemas/Revision" }
        risk_score: { $ref: "#/components/schemas/NonnegativeDecimal" }
        warnings:
          { type: array, items: { type: string, maxLength: 500 }, maxItems: 64 }
        advisory_only: { type: boolean, enum: [true] }
        quality: { $ref: "#/components/schemas/QualityEvidence" }
        recorded_at: { $ref: "#/components/schemas/Timestamp" }
        revision: { $ref: "#/components/schemas/Revision" }
    RebalancingPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [snapshot_revision, execution_available, items]
          properties:
            snapshot_revision: { $ref: "#/components/schemas/Revision" }
            execution_available: { type: boolean, enum: [false] }
            items:
              {
                type: array,
                items: { $ref: "#/components/schemas/RebalancingSummary" },
              }
    RebalancingRouteStep:
      type: object
      additionalProperties: false
      required:
        [
          index,
          role,
          fact_id,
          fact_version,
          from_exchange,
          from_asset,
          to_exchange,
          to_asset,
          expected_cost,
          minimum_duration_nanos,
          maximum_duration_nanos,
          confidence,
          approved,
          warnings,
          provenance_hash,
        ]
      properties:
        index: { type: integer, minimum: 0 }
        role: { type: string, enum: [trade, transfer] }
        fact_id: { type: string }
        fact_version: { $ref: "#/components/schemas/Revision" }
        from_exchange: { type: string }
        from_asset: { type: string }
        to_exchange: { type: string }
        to_asset: { type: string }
        network: { type: string }
        expected_cost: { $ref: "#/components/schemas/NonnegativeDecimal" }
        minimum_duration_nanos: { $ref: "#/components/schemas/Revision" }
        maximum_duration_nanos: { $ref: "#/components/schemas/Revision" }
        confidence: { $ref: "#/components/schemas/NonnegativeDecimal" }
        approved: { type: boolean }
        warnings:
          { type: array, items: { type: string, maxLength: 500 }, maxItems: 64 }
        provenance_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
    ManualChecklistStep:
      type: object
      additionalProperties: false
      required: [index, instruction, manual_only]
      properties:
        index: { type: integer, minimum: 0 }
        instruction: { type: string, minLength: 1, maxLength: 1000 }
        manual_only: { type: boolean, enum: [true] }
    RebalancingDetail:
      type: object
      additionalProperties: false
      required: [summary, route, checklist, execution_available]
      properties:
        summary: { $ref: "#/components/schemas/RebalancingSummary" }
        route:
          {
            type: array,
            items: { $ref: "#/components/schemas/RebalancingRouteStep" },
          }
        checklist:
          {
            type: array,
            items: { $ref: "#/components/schemas/ManualChecklistStep" },
          }
        execution_available: { type: boolean, enum: [false] }
    ChampionChallengerReport:
      type: object
      additionalProperties: false
      required:
        [
          id,
          champion_strategy_version,
          challenger_strategy_version,
          disposition,
          confidence,
          viability,
          disclaimer,
          manifest_hash,
          created_at,
          revision,
        ]
      properties:
        id: { type: string }
        champion_strategy_version: { type: string }
        challenger_strategy_version: { type: string }
        champion_suite_id: { type: string }
        challenger_suite_id: { type: string }
        disposition: { type: string }
        confidence: { type: string }
        viability: { type: string }
        disclaimer: { type: string }
        manifest_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
        created_at: { $ref: "#/components/schemas/Timestamp" }
        revision: { $ref: "#/components/schemas/Revision" }
    ChampionChallengerPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [snapshot_revision, items]
          properties:
            snapshot_revision: { $ref: "#/components/schemas/Revision" }
            items:
              {
                type: array,
                items:
                  { $ref: "#/components/schemas/ChampionChallengerReport" },
              }
    ReplayFaultKind:
      type: string
      enum:
        [
          disconnect,
          sequence_gap,
          latency,
          rejection,
          partial_fill,
          cancel_fill_race,
          unknown_state,
          storage_failure,
          restart_at_event,
        ]
    ReplayFaultRequest:
      type: object
      additionalProperties: false
      required: [expected_revision, fault, ordinal, delay_nanos, reason]
      properties:
        expected_revision: { $ref: "#/components/schemas/Revision" }
        fault: { $ref: "#/components/schemas/ReplayFaultKind" }
        ordinal: { $ref: "#/components/schemas/Revision" }
        delay_nanos: { $ref: "#/components/schemas/Revision" }
        repeatable: { type: boolean, default: false }
        reason: { type: string, minLength: 8, maxLength: 500 }
    ReplayFaultResource:
      type: object
      additionalProperties: false
      required:
        [
          id,
          replay_id,
          fault,
          ordinal,
          delay_nanos,
          repeatable,
          simulation_only,
          revision,
          created_at,
        ]
      properties:
        id: { type: string }
        replay_id: { type: string }
        fault: { $ref: "#/components/schemas/ReplayFaultKind" }
        ordinal: { $ref: "#/components/schemas/Revision" }
        delay_nanos: { $ref: "#/components/schemas/Revision" }
        repeatable: { type: boolean }
        simulation_only: { type: boolean, enum: [true] }
        revision: { $ref: "#/components/schemas/Revision" }
        created_at: { $ref: "#/components/schemas/Timestamp" }
    ReplayFaultPage:
      type: object
      additionalProperties: false
      required: [revision, simulation_only, items]
      properties:
        revision: { $ref: "#/components/schemas/Revision" }
        simulation_only: { type: boolean, enum: [true] }
        items:
          {
            type: array,
            items: { $ref: "#/components/schemas/ReplayFaultResource" },
          }
    ReportExportRequest:
      type: object
      additionalProperties: false
      required: [format]
      properties:
        format: { type: string, enum: [json, csv] }
    ReportExportResource:
      type: object
      additionalProperties: false
      required:
        [
          id,
          report_id,
          format,
          content_type,
          content,
          payload_hash,
          simulation_only,
          created_at,
          revision,
        ]
      properties:
        id: { type: string }
        report_id: { type: string }
        format: { type: string, enum: [json, csv] }
        content_type: { type: string, enum: [application/json, text/csv] }
        content: { type: string, maxLength: 1048576 }
        payload_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
        simulation_only: { type: boolean, enum: [true] }
        created_at: { $ref: "#/components/schemas/Timestamp" }
        revision: { $ref: "#/components/schemas/Revision" }
    PortfolioSummary:
      type: object
      additionalProperties: false
      required: [id, label, mode, equity, available, reserved, revision]
      properties:
        id: { type: string }
        label: { type: string, enum: [VIRTUAL] }
        mode: { type: string, enum: [backtest, replay, paper, shadow] }
        equity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        available: { $ref: "#/components/schemas/NonnegativeDecimal" }
        reserved: { $ref: "#/components/schemas/NonnegativeDecimal" }
        revision: { $ref: "#/components/schemas/Revision" }
    PortfolioPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [items]
          properties:
            items:
              {
                type: array,
                items: { $ref: "#/components/schemas/PortfolioSummary" },
              }
    Balance:
      type: object
      additionalProperties: false
      required: [asset, available, reserved]
      properties:
        asset: { type: string }
        available: { $ref: "#/components/schemas/NonnegativeDecimal" }
        reserved: { $ref: "#/components/schemas/NonnegativeDecimal" }
    Position:
      type: object
      additionalProperties: false
      required:
        [instrument, quantity, average_cost, unrealized_pnl, realized_pnl, fees]
      properties:
        instrument: { type: string }
        quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        average_cost: { $ref: "#/components/schemas/NonnegativeDecimal" }
        unrealized_pnl: { $ref: "#/components/schemas/Decimal" }
        realized_pnl: { $ref: "#/components/schemas/Decimal" }
        fees: { $ref: "#/components/schemas/NonnegativeDecimal" }
    PortfolioDetail:
      allOf:
        - $ref: "#/components/schemas/PortfolioSummary"
        - type: object
          required: [balances, positions, updated_at]
          properties:
            balances:
              { type: array, items: { $ref: "#/components/schemas/Balance" } }
            positions:
              { type: array, items: { $ref: "#/components/schemas/Position" } }
            updated_at: { $ref: "#/components/schemas/Timestamp" }
    JournalEntry:
      type: object
      additionalProperties: false
      required:
        [
          id,
          transaction_id,
          asset,
          direction,
          quantity,
          occurred_at,
          correlation_id,
        ]
      properties:
        id: { type: string }
        transaction_id: { type: string }
        asset: { type: string }
        direction: { type: string, enum: [debit, credit] }
        quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        occurred_at: { $ref: "#/components/schemas/Timestamp" }
        correlation_id: { type: string }
    JournalPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [virtual, items]
          properties:
            virtual: { type: boolean, enum: [true] }
            items:
              {
                type: array,
                items: { $ref: "#/components/schemas/JournalEntry" },
              }
    RiskContributor:
      type: object
      additionalProperties: false
      required: [name, usage, limit, reason_code]
      properties:
        name: { type: string }
        usage: { $ref: "#/components/schemas/NonnegativeDecimal" }
        limit: { $ref: "#/components/schemas/NonnegativeDecimal" }
        reason_code: { type: string }
    RiskStatus:
      type: object
      additionalProperties: false
      required:
        [
          state,
          revision,
          policy_version,
          contributors,
          recovery_ready,
          updated_at,
        ]
      properties:
        state: { type: string, enum: [NORMAL, CAUTIOUS, PAUSED, LOCKED] }
        revision: { $ref: "#/components/schemas/Revision" }
        policy_version: { $ref: "#/components/schemas/Revision" }
        reason_codes: { type: array, items: { type: string }, maxItems: 64 }
        contributors:
          {
            type: array,
            items: { $ref: "#/components/schemas/RiskContributor" },
          }
        recovery_ready: { type: boolean }
        unresolved_critical: { type: integer, minimum: 0 }
        updated_at: { $ref: "#/components/schemas/Timestamp" }
    RevisionCommandRequest:
      type: object
      additionalProperties: false
      required: [expected_revision, reason]
      properties:
        expected_revision: { $ref: "#/components/schemas/Revision" }
        reason: { type: string, minLength: 8, maxLength: 500 }
    CommandAccepted:
      type: object
      additionalProperties: false
      required: [id, state, target_id, revision, correlation_id, created_at]
      properties:
        id: { type: string }
        state: { type: string, enum: [pending, applied, rejected, failed] }
        target_id: { type: string }
        revision: { $ref: "#/components/schemas/Revision" }
        correlation_id: { type: string }
        created_at: { $ref: "#/components/schemas/Timestamp" }
    TrendParameter:
      type: object
      additionalProperties: false
      required: [id, value, unit, cadence, mutability]
      properties:
        id: { type: string }
        value: { type: string }
        unit: { type: string }
        cadence: { type: string }
        mutability: { type: string, enum: [immutable_per_run] }
    TrendStatus:
      type: object
      additionalProperties: false
      required:
        [version, timeframe, health, evidence_maturity, parameters, revision]
      properties:
        version: { type: string, enum: [trend.v1a.1] }
        timeframe: { type: string, enum: [4h] }
        health:
          { type: string, enum: [healthy, warming, paused, degraded, locked] }
        evidence_maturity:
          {
            type: string,
            enum: [local_tier_b, formal_tier_a, insufficient, rejected],
          }
        viability:
          {
            type: string,
            enum: [undetermined, viable_for_more_research, rejected],
          }
        parameters:
          {
            type: array,
            minItems: 16,
            maxItems: 16,
            items: { $ref: "#/components/schemas/TrendParameter" },
          }
        revision: { $ref: "#/components/schemas/Revision" }
    TrendDecision:
      type: object
      additionalProperties: false
      required:
        [
          id,
          outcome,
          reason_code,
          explanation,
          candle_view_id,
          market_view_id,
          occurred_at,
          revision,
        ]
      properties:
        id: { type: string }
        outcome: { type: string, enum: [accepted, rejected] }
        reason_code: { type: string }
        explanation: { type: string, maxLength: 2000 }
        candle_view_id: { type: string }
        market_view_id: { type: string }
        occurred_at: { $ref: "#/components/schemas/Timestamp" }
        revision: { $ref: "#/components/schemas/Revision" }
    TrendDecisionPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [items]
          properties:
            items:
              {
                type: array,
                items: { $ref: "#/components/schemas/TrendDecision" },
              }
    OfflineJobRequest:
      type: object
      additionalProperties: false
      required:
        [
          dataset_id,
          strategy_version,
          configuration_id,
          research_generation_id,
          root_seed_hash,
        ]
      properties:
        dataset_id: { type: string }
        strategy_version: { type: string, enum: [trend.v1a.1] }
        configuration_id: { type: string }
        research_generation_id: { type: string }
        root_seed_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
    ReplayJobRequest:
      allOf:
        - $ref: "#/components/schemas/OfflineJobRequest"
        - type: object
          properties:
            speed: { type: string, enum: [original, accelerated, maximum] }
            incident_id: { type: string }
            first_ordinal: { $ref: "#/components/schemas/Revision" }
            last_ordinal: { $ref: "#/components/schemas/Revision" }
    JobResult:
      type: object
      additionalProperties: false
      required:
        [
          result_hash,
          platform_correctness,
          strategy_evidence,
          viability,
          reproducibility,
          report_id,
          report_hash,
          confidence_label,
          research_coverage,
          disclaimer,
        ]
      properties:
        result_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
        platform_correctness: { type: string }
        strategy_evidence: { type: string }
        viability:
          {
            type: string,
            enum: [undetermined, viable_for_more_research, rejected],
          }
        reproducibility: { type: string }
        report_id: { type: string }
        report_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
        confidence_label:
          {
            type: string,
            enum: [local_tier_b, formal_tier_a, insufficient, rejected],
          }
        research_coverage:
          {
            type: string,
            enum: [single_run_incomplete, registered_suite_complete],
          }
        disclaimer: { type: string }
        metrics:
          type: object
          additionalProperties: { $ref: "#/components/schemas/Decimal" }
    ResearchResultSlice:
      type: object
      additionalProperties: false
      required: [name, net_return, max_drawdown, trades]
      properties:
        name: { type: string, minLength: 1 }
        net_return: { $ref: "#/components/schemas/Decimal" }
        max_drawdown: { $ref: "#/components/schemas/NonnegativeDecimal" }
        trades: { type: integer, format: int64, minimum: 0 }
    ResearchCapacityPoint:
      type: object
      additionalProperties: false
      required: [notional, net_return, fill_rate]
      properties:
        notional: { $ref: "#/components/schemas/NonnegativeDecimal" }
        net_return: { $ref: "#/components/schemas/Decimal" }
        fill_rate: { $ref: "#/components/schemas/NonnegativeDecimal" }
    RegisteredResearchReport:
      type: object
      additionalProperties: false
      required:
        [
          id,
          research_generation_id,
          manifest_hash,
          confidence_label,
          platform_correctness,
          strategy_evidence,
          viability,
          disclaimer,
          run_references,
          benchmarks,
          stress,
          capacity,
          canonical_manifest,
          created_at,
        ]
      properties:
        id: { type: string, minLength: 1 }
        research_generation_id: { type: string, minLength: 1 }
        manifest_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
        confidence_label:
          { type: string, enum: [local_tier_b, formal_tier_a, rejected] }
        platform_correctness: { type: string }
        strategy_evidence: { type: string }
        viability:
          {
            type: string,
            enum: [undetermined, viable_for_more_research, rejected],
          }
        disclaimer: { type: string }
        run_references:
          type: array
          items: { type: string, minLength: 1 }
        benchmarks:
          type: array
          items: { $ref: "#/components/schemas/ResearchResultSlice" }
        stress:
          type: array
          items: { $ref: "#/components/schemas/ResearchResultSlice" }
        capacity:
          type: array
          items: { $ref: "#/components/schemas/ResearchCapacityPoint" }
        canonical_manifest: { type: string, minLength: 2, maxLength: 1048576 }
        created_at: { $ref: "#/components/schemas/Timestamp" }
    JobResource:
      type: object
      additionalProperties: false
      required: [id, kind, state, mode_label, revision, created_at]
      properties:
        id: { type: string }
        kind: { type: string, enum: [backtest, replay] }
        state:
          {
            type: string,
            enum:
              [
                QUEUED,
                RUNNING,
                PAUSE_REQUESTED,
                PAUSED,
                CANCEL_REQUESTED,
                CANCELED,
                SUCCEEDED,
                FAILED,
              ],
          }
        mode_label: { type: string, enum: [BACKTEST, REPLAY] }
        revision: { $ref: "#/components/schemas/Revision" }
        progress: { $ref: "#/components/schemas/NonnegativeDecimal" }
        cursor_ordinal: { $ref: "#/components/schemas/Revision" }
        failure_code: { type: string }
        result: { $ref: "#/components/schemas/JobResult" }
        registered_report:
          { $ref: "#/components/schemas/RegisteredResearchReport" }
        replay_inspection:
          { $ref: "#/components/schemas/ReplayEventInspection" }
        created_at: { $ref: "#/components/schemas/Timestamp" }
        updated_at: { $ref: "#/components/schemas/Timestamp" }
    ReplayEventInspection:
      type: object
      additionalProperties: false
      required:
        [
          event_count,
          ordinal,
          event_hash,
          canonical_event,
          canonical_decision,
          canonical_orders,
          canonical_execution_events,
          canonical_balances,
        ]
      properties:
        event_count: { $ref: "#/components/schemas/Revision" }
        ordinal: { $ref: "#/components/schemas/Revision" }
        event_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
        canonical_event: { type: string, minLength: 2, maxLength: 1048576 }
        canonical_decision: { type: string, minLength: 2, maxLength: 1048576 }
        canonical_orders: { type: string, minLength: 2, maxLength: 1048576 }
        canonical_execution_events:
          { type: string, minLength: 2, maxLength: 1048576 }
        canonical_balances: { type: string, minLength: 2, maxLength: 1048576 }
    ShadowSessionRequest:
      type: object
      additionalProperties: false
      required: [configuration_id, strategy_version, portfolio_id]
      properties:
        configuration_id: { type: string }
        strategy_version: { type: string, enum: [trend.v1a.1] }
        portfolio_id: { type: string }
    SimulatedOrder:
      type: object
      additionalProperties: false
      required: [id, instrument, side, state, quantity, limit_price, simulated]
      properties:
        id: { type: string }
        instrument: { type: string }
        side: { type: string, enum: [buy, sell] }
        state: { type: string }
        quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        limit_price: { $ref: "#/components/schemas/NonnegativeDecimal" }
        filled_quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        latency_ms: { type: string, pattern: "^(0|[1-9][0-9]*)$" }
        simulated: { type: boolean, enum: [true] }
    ShadowSessionResource:
      type: object
      additionalProperties: false
      required:
        [
          id,
          state,
          label,
          public_only,
          simulation_only,
          entries_enabled,
          revision,
          created_at,
          configuration_id,
          strategy_version,
          decision_dataset_id,
          model_namespace_id,
          accepted_decisions,
          rejected_decisions,
          journal_transactions,
        ]
      properties:
        id: { type: string }
        state:
          {
            type: string,
            enum: [QUEUED, RUNNING, PAUSED, CANCEL_REQUESTED, CANCELED, FAILED],
          }
        label: { type: string, enum: [PUBLIC-LIVE SHADOW / VIRTUAL] }
        public_only: { type: boolean, enum: [true] }
        simulation_only: { type: boolean, enum: [true] }
        entries_enabled: { type: boolean }
        revision: { $ref: "#/components/schemas/Revision" }
        configuration_id: { type: string }
        strategy_version: { type: string }
        decision_dataset_id: { type: string }
        model_namespace_id: { type: string }
        accepted_decisions: { type: integer, minimum: 0 }
        rejected_decisions: { type: integer, minimum: 0 }
        journal_transactions: { type: integer, minimum: 0 }
        risk_state: { type: string, enum: [PAUSED, RESUMED, LOCKED] }
        orders:
          {
            type: array,
            items: { $ref: "#/components/schemas/SimulatedOrder" },
          }
        created_at: { $ref: "#/components/schemas/Timestamp" }
        started_at: { $ref: "#/components/schemas/Timestamp" }
        stopped_at: { $ref: "#/components/schemas/Timestamp" }
        failure_code: { type: string }
    IncidentSummary:
      type: object
      additionalProperties: false
      required: [id, severity, state, reason_code, opened_at, revision]
      properties:
        id: { type: string }
        severity: { type: string, enum: [warning, error, critical] }
        state: { type: string, enum: [open, acknowledged, resolved] }
        reason_code: { type: string }
        opened_at: { $ref: "#/components/schemas/Timestamp" }
        revision: { $ref: "#/components/schemas/Revision" }
    IncidentPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [items]
          properties:
            items:
              {
                type: array,
                items: { $ref: "#/components/schemas/IncidentSummary" },
              }
    TimelineEvent:
      type: object
      additionalProperties: false
      required: [id, event_type, occurred_at, correlation_id, redacted]
      properties:
        id: { type: string }
        event_type: { type: string }
        occurred_at: { $ref: "#/components/schemas/Timestamp" }
        correlation_id: { type: string }
        redacted: { type: boolean }
        safe_detail: { type: string, maxLength: 2000 }
    IncidentDetail:
      allOf:
        - $ref: "#/components/schemas/IncidentSummary"
        - type: object
          required: [timeline, replay_window]
          properties:
            timeline:
              {
                type: array,
                items: { $ref: "#/components/schemas/TimelineEvent" },
              }
            replay_window:
              type: object
              additionalProperties: false
              required: [dataset_id, first_ordinal, last_ordinal]
              properties:
                dataset_id: { type: string }
                first_ordinal: { $ref: "#/components/schemas/Revision" }
                last_ordinal: { $ref: "#/components/schemas/Revision" }
    AuditEvent:
      type: object
      additionalProperties: false
      required:
        [
          id,
          event_type,
          actor,
          correlation_id,
          causation_id,
          recorded_at,
          redacted,
        ]
      properties:
        id: { type: string }
        event_type: { type: string }
        actor: { type: string }
        correlation_id: { type: string }
        causation_id: { type: string }
        recorded_at: { $ref: "#/components/schemas/Timestamp" }
        redacted: { type: boolean }
        safe_detail: { type: string, maxLength: 2000 }
    AuditEventPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [items]
          properties:
            items:
              {
                type: array,
                items: { $ref: "#/components/schemas/AuditEvent" },
              }
    SandboxExchange:
      type: string
      enum: [binance, bybit]
      x-enum-varnames: [SandboxExchangeBinance, SandboxExchangeBybit]
    SandboxEnvironment:
      type: string
      enum: [spot_testnet, demo]
      x-enum-varnames: [SandboxEnvironmentSpotTestnet, SandboxEnvironmentDemo]
    SandboxCapUsage:
      type: object
      additionalProperties: false
      required:
        [
          utc_day,
          per_order_limit,
          daily_limit,
          daily_reserved,
          daily_remaining,
          account_open,
          account_open_limit,
          global_open,
          global_open_limit,
        ]
      properties:
        utc_day: { type: string, format: date }
        per_order_limit: { $ref: "#/components/schemas/NonnegativeDecimal" }
        daily_limit: { $ref: "#/components/schemas/NonnegativeDecimal" }
        daily_reserved: { $ref: "#/components/schemas/NonnegativeDecimal" }
        daily_remaining: { $ref: "#/components/schemas/NonnegativeDecimal" }
        account_open: { type: integer, minimum: 0 }
        account_open_limit:
          type: integer
          enum: [1]
          x-enum-varnames: [SandboxCapUsageAccountOpenLimitOne]
        global_open: { type: integer, minimum: 0 }
        global_open_limit:
          type: integer
          enum: [2]
          x-enum-varnames: [SandboxCapUsageGlobalOpenLimitTwo]
    SandboxArm:
      type: object
      additionalProperties: false
      required:
        [
          id,
          session_id,
          account_ids,
          state,
          created_at,
          expires_at,
          revision,
          audit_url,
        ]
      properties:
        id: { type: string, minLength: 1 }
        session_id: { type: string, minLength: 1 }
        account_ids:
          type: array
          minItems: 1
          maxItems: 2
          items: { type: string, minLength: 1 }
        state:
          type: string
          enum: [active, expired, revoked]
          x-enum-varnames:
            [
              SandboxArmStateActive,
              SandboxArmStateExpired,
              SandboxArmStateRevoked,
            ]
        created_at: { $ref: "#/components/schemas/Timestamp" }
        expires_at: { $ref: "#/components/schemas/Timestamp" }
        revoked_at: { $ref: "#/components/schemas/Timestamp" }
        revision: { $ref: "#/components/schemas/Revision" }
        audit_url: { type: string, pattern: "^/api/v1/audit-events" }
    SandboxAccount:
      type: object
      additionalProperties: false
      required:
        [
          id,
          exchange,
          environment,
          state,
          engine_ready,
          account_epoch,
          credential_generation,
          revision,
          startup_cycle,
          private_stream_healthy,
          reconciliation_clean,
          evidence_healthy,
          lease_held,
          observed_at,
          stale,
          cap_usage,
          audit_url,
        ]
      properties:
        id: { type: string, minLength: 1 }
        exchange: { $ref: "#/components/schemas/SandboxExchange" }
        environment: { $ref: "#/components/schemas/SandboxEnvironment" }
        state:
          type: string
          enum: [LOCKED, READY_PAUSED, ARMED, DEGRADED, QUARANTINED]
          x-enum-varnames:
            [
              SandboxAccountStateLocked,
              SandboxAccountStateReadyPaused,
              SandboxAccountStateArmed,
              SandboxAccountStateDegraded,
              SandboxAccountStateQuarantined,
            ]
        engine_ready: { type: boolean }
        account_epoch: { type: integer, format: int64, minimum: 1 }
        credential_generation: { type: integer, format: int64, minimum: 1 }
        revision: { $ref: "#/components/schemas/Revision" }
        startup_cycle: { type: integer, format: int64, minimum: 0 }
        private_stream_healthy: { type: boolean }
        reconciliation_clean: { type: boolean }
        evidence_healthy: { type: boolean }
        lease_held: { type: boolean }
        observed_at: { $ref: "#/components/schemas/Timestamp" }
        stale: { type: boolean }
        session_id: { type: string, minLength: 1 }
        session_revision: { $ref: "#/components/schemas/Revision" }
        active_arm: { $ref: "#/components/schemas/SandboxArm" }
        cap_usage: { $ref: "#/components/schemas/SandboxCapUsage" }
        audit_url: { type: string, pattern: "^/api/v1/audit-events" }
    SandboxFill:
      type: object
      additionalProperties: false
      required:
        [
          id,
          order_id,
          quantity,
          price,
          fee_quantity,
          fee_asset,
          occurred_at,
          audit_url,
        ]
      properties:
        id: { type: string, pattern: "^[0-9a-f]{64}$" }
        order_id: { type: string, minLength: 1 }
        quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        price: { $ref: "#/components/schemas/NonnegativeDecimal" }
        fee_quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        fee_asset:
          type: string
          enum: [USDT, BTC, ETH]
          x-enum-varnames:
            [
              SandboxFillFeeAssetUSDT,
              SandboxFillFeeAssetBTC,
              SandboxFillFeeAssetETH,
            ]
        occurred_at: { $ref: "#/components/schemas/Timestamp" }
        audit_url: { type: string, pattern: "^/api/v1/audit-events" }
    SandboxOrder:
      type: object
      additionalProperties: false
      required:
        [
          id,
          account_id,
          exchange,
          environment,
          state,
          action,
          instrument,
          side,
          quantity,
          limit_price,
          notional,
          style,
          attempt,
          recovery_status,
          created_at,
          updated_at,
          revision,
          fills,
          audit_url,
        ]
      properties:
        id: { type: string, minLength: 1 }
        account_id: { type: string, minLength: 1 }
        exchange: { $ref: "#/components/schemas/SandboxExchange" }
        environment: { $ref: "#/components/schemas/SandboxEnvironment" }
        state:
          type: string
          enum:
            [
              APPROVED,
              SUBMITTING,
              ACKNOWLEDGED,
              PARTIALLY_FILLED,
              FILLED,
              CANCEL_PENDING,
              CANCELED,
              REJECTED,
              EXPIRED,
              UNKNOWN,
              RECOVERY_REQUIRED,
            ]
          x-enum-varnames:
            [
              SandboxOrderStateApproved,
              SandboxOrderStateSubmitting,
              SandboxOrderStateAcknowledged,
              SandboxOrderStatePartiallyFilled,
              SandboxOrderStateFilled,
              SandboxOrderStateCancelPending,
              SandboxOrderStateCanceled,
              SandboxOrderStateRejected,
              SandboxOrderStateExpired,
              SandboxOrderStateUnknown,
              SandboxOrderStateRecoveryRequired,
            ]
        action:
          type: string
          enum: [ENTRY, EXIT, CANCEL, RECOVERY]
          x-enum-varnames:
            [
              SandboxOrderActionEntry,
              SandboxOrderActionExit,
              SandboxOrderActionCancel,
              SandboxOrderActionRecovery,
            ]
        instrument:
          type: string
          enum: [BTCUSDT, ETHUSDT, ETHBTC]
          x-enum-varnames:
            [
              SandboxOrderInstrumentBTCUSDT,
              SandboxOrderInstrumentETHUSDT,
              SandboxOrderInstrumentETHBTC,
            ]
        side:
          type: string
          enum: [buy, sell]
          x-enum-varnames: [SandboxOrderSideBuy, SandboxOrderSideSell]
        quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        limit_price: { $ref: "#/components/schemas/NonnegativeDecimal" }
        notional: { $ref: "#/components/schemas/NonnegativeDecimal" }
        style:
          type: string
          enum: [LIMIT_GTC, LIMIT_IOC, POST_ONLY]
          x-enum-varnames:
            [
              SandboxOrderStyleLimitGTC,
              SandboxOrderStyleLimitIOC,
              SandboxOrderStylePostOnly,
            ]
        attempt: { type: integer, minimum: 0 }
        recovery_status:
          type: string
          enum: [not_required, required, querying, reconciled]
          x-enum-varnames:
            [
              SandboxOrderRecoveryStatusNotRequired,
              SandboxOrderRecoveryStatusRequired,
              SandboxOrderRecoveryStatusQuerying,
              SandboxOrderRecoveryStatusReconciled,
            ]
        unknown_since: { $ref: "#/components/schemas/Timestamp" }
        created_at: { $ref: "#/components/schemas/Timestamp" }
        updated_at: { $ref: "#/components/schemas/Timestamp" }
        revision: { $ref: "#/components/schemas/Revision" }
        fills:
          type: array
          items: { $ref: "#/components/schemas/SandboxFill" }
        audit_url: { type: string, pattern: "^/api/v1/audit-events" }
    SandboxOrderPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [items]
          properties:
            items:
              type: array
              items: { $ref: "#/components/schemas/SandboxOrder" }
    SandboxDifference:
      type: object
      additionalProperties: false
      required:
        [id, category, classification, critical, state, recorded_at, audit_url]
      properties:
        id: { type: string, minLength: 1 }
        category: { type: string, minLength: 1 }
        classification: { type: string, minLength: 1 }
        asset:
          type: string
          enum: [USDT, BTC, ETH]
          x-enum-varnames:
            [
              SandboxDifferenceAssetUSDT,
              SandboxDifferenceAssetBTC,
              SandboxDifferenceAssetETH,
            ]
        quantity: { $ref: "#/components/schemas/Decimal" }
        critical: { type: boolean }
        state:
          type: string
          enum: [OPEN, RESOLVED, QUARANTINED, ADJUSTED]
          x-enum-varnames:
            [
              SandboxDifferenceStateOpen,
              SandboxDifferenceStateResolved,
              SandboxDifferenceStateQuarantined,
              SandboxDifferenceStateAdjusted,
            ]
        recorded_at: { $ref: "#/components/schemas/Timestamp" }
        audit_url: { type: string, pattern: "^/api/v1/audit-events" }
    SandboxReconciliation:
      type: object
      additionalProperties: false
      required:
        [
          id,
          account_id,
          exchange,
          account_epoch,
          state,
          reconciled_at,
          differences,
          suspense_count,
          quarantine_count,
          audit_url,
        ]
      properties:
        id: { type: string, minLength: 1 }
        account_id: { type: string, minLength: 1 }
        exchange: { $ref: "#/components/schemas/SandboxExchange" }
        account_epoch: { type: integer, format: int64, minimum: 1 }
        state:
          type: string
          enum: [clean, quarantined]
          x-enum-varnames:
            [
              SandboxReconciliationStateClean,
              SandboxReconciliationStateQuarantined,
            ]
        reconciled_at: { $ref: "#/components/schemas/Timestamp" }
        differences:
          type: array
          items: { $ref: "#/components/schemas/SandboxDifference" }
        suspense_count: { type: integer, minimum: 0 }
        quarantine_count: { type: integer, minimum: 0 }
        audit_url: { type: string, pattern: "^/api/v1/audit-events" }
    SandboxResetIncident:
      type: object
      additionalProperties: false
      required:
        [
          id,
          account_id,
          exchange,
          prior_epoch,
          new_epoch,
          state,
          detected_at,
          adjustments,
          audit_url,
        ]
      properties:
        id: { type: string, minLength: 1 }
        account_id: { type: string, minLength: 1 }
        exchange: { $ref: "#/components/schemas/SandboxExchange" }
        prior_epoch: { type: integer, format: int64, minimum: 1 }
        new_epoch: { type: integer, format: int64, minimum: 2 }
        state:
          type: string
          enum: [OPEN, RECONCILING, RESOLVED, QUARANTINED]
          x-enum-varnames:
            [
              SandboxResetIncidentStateOpen,
              SandboxResetIncidentStateReconciling,
              SandboxResetIncidentStateResolved,
              SandboxResetIncidentStateQuarantined,
            ]
        detected_at: { $ref: "#/components/schemas/Timestamp" }
        resolved_at: { $ref: "#/components/schemas/Timestamp" }
        adjustments:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [asset, quantity, pnl_effect, recorded_at]
            properties:
              asset:
                type: string
                enum: [USDT, BTC, ETH]
                x-enum-varnames:
                  [
                    SandboxResetIncidentAdjustmentAssetUSDT,
                    SandboxResetIncidentAdjustmentAssetBTC,
                    SandboxResetIncidentAdjustmentAssetETH,
                  ]
              quantity: { $ref: "#/components/schemas/Decimal" }
              pnl_effect: { type: boolean, enum: [false] }
              recorded_at: { $ref: "#/components/schemas/Timestamp" }
        audit_url: { type: string, pattern: "^/api/v1/audit-events" }
    SandboxReconciliationPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          required: [items, reset_incidents]
          properties:
            items:
              type: array
              items: { $ref: "#/components/schemas/SandboxReconciliation" }
            reset_incidents:
              type: array
              items: { $ref: "#/components/schemas/SandboxResetIncident" }
    C6ChaosSummary:
      type: object
      additionalProperties: false
      required: [status, passed, failed, last_observed_at]
      properties:
        status:
          type: string
          enum: [not_run, passed, failed]
          x-enum-varnames:
            [
              C6ChaosSummaryStatusNotRun,
              C6ChaosSummaryStatusPassed,
              C6ChaosSummaryStatusFailed,
            ]
        passed: { type: integer, minimum: 0 }
        failed: { type: integer, minimum: 0 }
        last_observed_at: { $ref: "#/components/schemas/Timestamp" }
    C6SLOSummary:
      type: object
      additionalProperties: false
      required:
        [
          samples,
          critical_alert_latency_ms,
          recovery_duration_ms,
          duplicate_creates,
          lost_fills,
          double_posted_fills,
          unknown_orders,
          reconciliation_mismatches,
          suspense_items,
          reconnects,
          restarts,
          resident_memory_delta_bytes,
          positive_memory_leak_trend,
          passing,
        ]
      properties:
        samples: { type: integer, format: int64, minimum: 0 }
        critical_alert_latency_ms: { type: integer, format: int64, minimum: 0 }
        recovery_duration_ms: { type: integer, format: int64, minimum: 0 }
        duplicate_creates: { type: integer, format: int64, minimum: 0 }
        lost_fills: { type: integer, format: int64, minimum: 0 }
        double_posted_fills: { type: integer, format: int64, minimum: 0 }
        unknown_orders: { type: integer, format: int64, minimum: 0 }
        reconciliation_mismatches: { type: integer, format: int64, minimum: 0 }
        suspense_items: { type: integer, format: int64, minimum: 0 }
        reconnects: { type: integer, format: int64, minimum: 0 }
        restarts: { type: integer, format: int64, minimum: 0 }
        resident_memory_delta_bytes: { type: integer, format: int64 }
        positive_memory_leak_trend: { type: boolean }
        passing: { type: boolean }
    C6QualificationStatus:
      type: object
      additionalProperties: false
      required:
        [
          state,
          mode,
          required_duration_seconds,
          observed_duration_seconds,
          profitability_evidence,
          qualified,
          failures,
          chaos,
          slo,
          recovery_incidents,
          formal_soak_pending,
          audit_url,
        ]
      properties:
        id: { type: string }
        state:
          type: string
          enum: [not_started, PENDING, RUNNING, SMOKE_PASSED, PASSED, FAILED]
          x-enum-varnames:
            [
              C6QualificationStatusStateNotStarted,
              C6QualificationStatusStatePending,
              C6QualificationStatusStateRunning,
              C6QualificationStatusStateSmokePassed,
              C6QualificationStatusStatePassed,
              C6QualificationStatusStateFailed,
            ]
        mode:
          type: string
          enum: [none, smoke, formal]
          x-enum-varnames:
            [
              C6QualificationStatusModeNone,
              C6QualificationStatusModeSmoke,
              C6QualificationStatusModeFormal,
            ]
        commit_sha: { type: string, pattern: "^[0-9a-f]{40}$" }
        build_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
        executable_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
        image_hash: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        configuration_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
        started_at: { $ref: "#/components/schemas/Timestamp" }
        ended_at: { $ref: "#/components/schemas/Timestamp" }
        required_duration_seconds: { type: integer, format: int64, minimum: 0 }
        observed_duration_seconds: { type: integer, format: int64, minimum: 0 }
        profitability_evidence: { type: boolean, enum: [false] }
        qualified: { type: boolean }
        evidence_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
        failures:
          type: array
          items: { type: string, maxLength: 80 }
        chaos: { $ref: "#/components/schemas/C6ChaosSummary" }
        slo: { $ref: "#/components/schemas/C6SLOSummary" }
        recovery_incidents:
          type: array
          items: { $ref: "#/components/schemas/C6RecoveryIncident" }
        formal_soak_pending: { type: boolean }
        audit_url: { type: string, pattern: "^/api/v1/audit-events" }
    C6RecoveryIncident:
      type: object
      additionalProperties: false
      required:
        [
          account_id,
          exchange,
          environment,
          state,
          incident_source,
          reason_category,
          cause_code,
          deadline_at,
          clean_check_count,
          detected_at,
          evidence_hash,
        ]
      properties:
        account_id: { type: string, minLength: 1 }
        exchange:
          type: string
          enum: [binance, bybit]
          x-enum-varnames:
            [C6RecoveryIncidentExchangeBinance, C6RecoveryIncidentExchangeBybit]
        environment:
          type: string
          enum: [spot_testnet, demo]
          x-enum-varnames:
            [
              C6RecoveryIncidentEnvironmentSpotTestnet,
              C6RecoveryIncidentEnvironmentDemo,
            ]
        state:
          type: string
          enum: [active, recovered, expired, repeated, unrecoverable]
          x-enum-varnames:
            [
              C6RecoveryIncidentStateActive,
              C6RecoveryIncidentStateRecovered,
              C6RecoveryIncidentStateExpired,
              C6RecoveryIncidentStateRepeated,
              C6RecoveryIncidentStateUnrecoverable,
            ]
        incident_source:
          type: string
          enum: [reconciliation, private_stream]
          x-enum-varnames:
            [
              C6RecoveryIncidentIncidentSourceReconciliation,
              C6RecoveryIncidentIncidentSourcePrivateStream,
            ]
        reason_category: { type: string, maxLength: 64 }
        cause_code: { type: string, pattern: "^[a-z0-9_]{1,64}$" }
        deadline_at: { $ref: "#/components/schemas/Timestamp" }
        clean_check_count: { type: integer, minimum: 0, maximum: 2 }
        detected_at: { $ref: "#/components/schemas/Timestamp" }
        recovery_timestamp: { $ref: "#/components/schemas/Timestamp" }
        evidence_hash: { type: string, pattern: "^[0-9a-f]{64}$" }
    SandboxOverview:
      type: object
      additionalProperties: false
      required:
        [
          environment_label,
          real_trading_enabled,
          observed_at,
          stale,
          accounts,
          active_arms,
          orders,
          reconciliations,
          reset_incidents,
          risk_state,
          qualification,
          audit_url,
        ]
      properties:
        environment_label:
          type: string
          enum: [BINANCE SPOT TESTNET + BYBIT DEMO / VIRTUAL]
          x-enum-varnames: [SandboxOverviewEnvironmentLabelVirtualOnly]
        real_trading_enabled: { type: boolean, enum: [false] }
        observed_at: { $ref: "#/components/schemas/Timestamp" }
        stale: { type: boolean }
        accounts:
          type: array
          items: { $ref: "#/components/schemas/SandboxAccount" }
        active_arms:
          type: array
          items: { $ref: "#/components/schemas/SandboxArm" }
        orders:
          type: array
          items: { $ref: "#/components/schemas/SandboxOrder" }
        reconciliations:
          type: array
          items: { $ref: "#/components/schemas/SandboxReconciliation" }
        reset_incidents:
          type: array
          items: { $ref: "#/components/schemas/SandboxResetIncident" }
        risk_state:
          type: string
          enum: [NORMAL, CAUTIOUS, PAUSED, LOCKED]
          x-enum-varnames:
            [
              SandboxOverviewRiskStateNormal,
              SandboxOverviewRiskStateCautious,
              SandboxOverviewRiskStatePaused,
              SandboxOverviewRiskStateLocked,
            ]
        qualification: { $ref: "#/components/schemas/C6QualificationStatus" }
        audit_url: { type: string, pattern: "^/api/v1/audit-events" }
    SandboxAuthorizationRequest:
      type: object
      additionalProperties: false
      required: [purpose, password, totp, reason]
      properties:
        purpose:
          type: string
          enum: [sandbox_arm, risk_unlock]
          x-enum-varnames:
            [
              SandboxAuthorizationRequestPurposeSandboxArm,
              SandboxAuthorizationRequestPurposeRiskUnlock,
            ]
        password: { type: string, minLength: 12, maxLength: 1024 }
        totp: { type: string, pattern: "^[0-9]{6}$" }
        reason: { type: string, minLength: 8, maxLength: 500 }
    SandboxAuthorizationGrant:
      type: object
      additionalProperties: false
      required: [token, purpose, expires_at]
      properties:
        token: { type: string, minLength: 32 }
        purpose:
          type: string
          enum: [sandbox_arm, risk_unlock]
          x-enum-varnames:
            [
              SandboxAuthorizationGrantPurposeSandboxArm,
              SandboxAuthorizationGrantPurposeRiskUnlock,
            ]
        expires_at: { $ref: "#/components/schemas/Timestamp" }
    SandboxArmRequest:
      type: object
      additionalProperties: false
      required: [authorization_token, expected_revision, account_ids, reason]
      properties:
        authorization_token: { type: string, minLength: 32 }
        expected_revision: { $ref: "#/components/schemas/Revision" }
        account_ids:
          type: array
          minItems: 1
          maxItems: 2
          uniqueItems: true
          items: { type: string, minLength: 1 }
        reason: { type: string, minLength: 8, maxLength: 500 }
    SandboxUnlockRequest:
      type: object
      additionalProperties: false
      required:
        [authorization_token, expected_revision, reconciliation_id, reason]
      properties:
        authorization_token: { type: string, minLength: 32 }
        expected_revision: { $ref: "#/components/schemas/Revision" }
        reconciliation_id: { type: string, minLength: 1 }
        reason: { type: string, minLength: 8, maxLength: 500 }
    SandboxTestOrderRequest:
      type: object
      additionalProperties: false
      required:
        [
          session_id,
          arm_id,
          account_id,
          exchange,
          instrument,
          side,
          quantity,
          limit_price,
          style,
          expected_revision,
          reason,
        ]
      properties:
        session_id: { type: string, minLength: 1 }
        arm_id: { type: string, minLength: 1 }
        account_id: { type: string, minLength: 1 }
        exchange: { $ref: "#/components/schemas/SandboxExchange" }
        instrument:
          type: string
          enum: [BTCUSDT, ETHUSDT]
          x-enum-varnames:
            [
              SandboxTestOrderRequestInstrumentBTCUSDT,
              SandboxTestOrderRequestInstrumentETHUSDT,
            ]
        side:
          type: string
          enum: [buy]
          x-enum-varnames: [SandboxTestOrderRequestSideBuy]
        quantity: { $ref: "#/components/schemas/NonnegativeDecimal" }
        limit_price: { $ref: "#/components/schemas/NonnegativeDecimal" }
        style:
          type: string
          enum: [LIMIT_GTC, LIMIT_IOC, POST_ONLY]
          x-enum-varnames:
            [
              SandboxTestOrderRequestStyleLimitGTC,
              SandboxTestOrderRequestStyleLimitIOC,
              SandboxTestOrderRequestStylePostOnly,
            ]
        expected_revision: { $ref: "#/components/schemas/Revision" }
        reason: { type: string, minLength: 8, maxLength: 500 }
    StreamEvent:
      type: object
      additionalProperties: false
      required:
        [
          id,
          stream,
          schema_version,
          revision,
          entity_revision,
          occurred_at,
          correlation_id,
          causation_id,
          event_type,
          payload,
        ]
      properties:
        id: { type: string }
        stream:
          {
            type: string,
            enum:
              [
                system,
                exchange,
                portfolio,
                risk,
                trend,
                job,
                shadow,
                incident,
                alert,
                order,
                fill,
                opportunity,
                strategy,
                inventory,
                rebalancing,
                research,
                sandbox,
              ],
          }
        schema_version: { type: string, enum: [axiom.stream.v1] }
        revision: { $ref: "#/components/schemas/Revision" }
        entity_revision: { $ref: "#/components/schemas/Revision" }
        occurred_at: { $ref: "#/components/schemas/Timestamp" }
        correlation_id: { type: string }
        causation_id: { type: string }
        event_type: { type: string }
        payload: { type: object, additionalProperties: true }
